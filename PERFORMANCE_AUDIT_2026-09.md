# 0.1.9.4 性能审查与源码优化记录

审查日期：2026-09-25
范围：当前工作区 C# 运行时、年度调度、可见角色视觉、UI、存档与现有性能采样。
约束：未启动 WorldBox，未做游戏内测试，未使用模拟人口或合成性能数据。

## 证据边界

当前目录没有可用的 Unity Profiler 会话或实际游戏运行采样。`release/worldbox-020-perf-load.log` 与 `release/worldbox-020-load.log` 是无图形启动/加载日志，未包含 FPS、主线程耗时或 GC Alloc 数据。因此本报告只把代码中可直接确认的工作量作为静态证据；不把它们称作已由 Profiler 证明的运行热点，也不伪造优化前后毫秒、FPS 或字节数。

项目已有两套可供之后实测的采样：

- `Core/MclslUnityProfiler.cs` 为 Unity Profiler 样本包装器；采样只有在 Unity Profiler 启用时可见。
- `Core/MclslPerformanceProbe.cs` 提供开发者包中的计时、调用次数、最长样本、帧时间、最低 FPS 和线程分配采样。它需要在游戏中显式启用。

两者已有年度角色阶段、新法转化、候选扫描、年度世界处理、地图标记和境界光环样本。当前缺少实际采样结果，不能据此推断谁是玩家机器上的真实最大 CPU 热点。

## 本轮确认并优化的问题

### 1. FPS 覆盖层每次 GUI 绘制都分配新字符串

`UI/MclslFpsOverlay.cs` 原先在 `OnGUI` 中每次执行 `"FPS: " + _displayFps`。Unity 可能在一帧中多次调用 `OnGUI`，导致显示开启时按 GUI 事件重复拼接字符串。

现在仅在 0.25 秒采样更新且 FPS 数值变化时重建文本；`OnGUI` 重用缓存字符串。此项减少的是明确存在的字符串构造次数，实际 GC Alloc 字节数尚未实测。

### 2. 境界光环对每个可见角色重复反射取同一位置数组

`Systems/Visual/MclslVisibleActorRenderLane.cs` 原先在可见角色循环中，对同一个 `ActorRenderData.positions` 字段逐人调用 `FieldInfo.GetValue`。现在在每轮可见扫描开始时读取一次，再按索引访问数组；取值失败时保留原有角色位置/地块位置回退。

静态工作量由每轮最多对 V 个可见角色反射读取 V 次，改为每轮一次反射读取（V 为有效可见列表长度）。扫描间隔、角色筛选、特效创建和回退路径不变。未对 CPU 时间作实测结论。

### 3. 角色资料滚动恢复每次刷新重复强制重建全局 Canvas

`UI/MclslActorInfoPanel.cs` 正文变化时先执行一次 `Canvas.ForceUpdateCanvases()` 和内容布局重建，然后最多 4 个后续 `LateUpdate` 中再次强制刷新全局 Canvas。后续刷新仅用于恢复滚动位置，不需要重复全局布局重建。

现在保留正文变化时的一次全局布局刷新和最多 4 帧的滚动位置恢复重试，移除后续重试里的强制 Canvas 刷新。每次有正文变化的恢复周期，强制 Canvas 刷新次数由最多 5 次减为 1 次。游戏内滚动效果未测试。

## 其他高频路径审查结果

- `Core/MclslRuntime.cs` 按 `Time.frameCount` 去重主循环；年度检查每 15 个运行帧触发一次，不是逐帧扫描全部修士。
- `Systems/Runtime/MclslScheduler.cs` 的候选索引刷新与年度入队使用数量和时间预算；修士按年度流水线阶段入队处理，并有单帧阶段预算。新法转化也有独立延期队列。没有改动其结算顺序或跳过角色。
- `Modules/MclslModuleHub.cs` 将年度模块工作限制为每帧最多 2 个模块、载入恢复最多 1 个模块；模块清单固定且较小。
- `Systems/Visual/MclslVisibleActorRenderLane.cs` 只检查游戏提供的可见角色数组，境界光环可见扫描每 10 帧一次；`MclslRealmHaloVisualSystem` 对活跃特效条目更新并以每轮 12 个条目清理过期项。样本已存在，但没有实测耗时。
- `Systems/Visual/MclslMapMarkerVisualSystem.cs` 使用 30 帧刷新节奏、数据版本/年份脏标记、复用的集合缓冲；动画另按 4 帧节奏更新。
- 排行榜窗口的 `Update` 只在窗口启用时运行；数据快照在打开/用户操作/显式刷新时建立，滚动只在可见卡片范围变化时重用/回收卡片。人物资料栏的全窗口刷新每 180 帧执行一次，角色定向刷新只在显式资料更新时扫描窗口。
- 天地之魄的帧处理每 4 帧执行，遍历档案中的天地之魄记录，不扫描全部修士。
- `MclslWorldArchiveStore` 仅在档案有脏数据且每 1800 帧到达保存点时序列化，不是逐帧写盘。事件、遗迹、生成物等多类档案记录有数量上限；序列化整份档案仍可能在超大存档的保存点产生主线程峰值，需要游戏采样确认。
- 新法纪元相关的年度、高频入口均有 Profiler/性能探针样本；LINQ 命中分布在图鉴快照、UI 和较低频数据维护中。没有把全仓库 LINQ 次数直接当作热点证据。

## 验证结果

- 正式 Release 构建：成功，0 警告，0 错误。
- 开发者工具 Release 构建：成功，0 警告，0 错误。
- 未启动游戏、未进行游戏内测试；没有实测 1000/3000/5000 人或 20 倍速数据。
- 因没有运行前后的 Profiler 捕获，本轮不能报告可核实的 FPS、主线程毫秒或 GC Alloc 差值。上文所列只是代码操作次数变化，不是性能基准。

## 尚待实际采样的问题

1. 在相同地图、视角、人口与 20 倍速下采集新法纪元 1000、3000、5000 人的年度切换、角色阶段、候选扫描、境界光环与存档样本。
2. 重点检查周期存档的序列化长帧，以及数千可见/活跃角色时的光环更新；没有采样前不宜异步改造存档或降低视觉更新频率。
3. 报告平均/峰值主线程毫秒、最低 FPS、每帧 GC Alloc、各样本调用次数及至少多轮重复结果，才能判断是否仍有值得进一步优化的热点。

本轮没有进行玩法削减、降低游戏速度、降低模拟精度或减少修士数量等优化。
